version: '3'
services:
    microservice-notification:
        image: esgimicroci/al2-notifications:latest
        restart: always
        ports:
            - "8187:8187"
        environment:
          - TOKENACL=${TOKENACL}
          - DiscordToken=${DiscordToken}
          - NotificationSiteUrl=${NotificationSiteUrl}
          - CONSUL_PORT=${CONSUL_PORT}
          - CONSUL_HOST=${CONSUL_HOST}
          - APPLICATION_PORT=${APPLICATION_PORT}
          - COMMANDS_RABBITMQ_HOST=${COMMANDS_RABBITMQ_HOST}
          - COMMANDS_RABBITMQ_PORT=${COMMANDS_RABBITMQ_PORT} 
          - COMMANDS_RABBITMQ_QUEUE=${COMMANDS_RABBITMQ_QUEUE}
          - COMMANDS_RABBITMQ_USERNAME=${COMMANDS_RABBITMQ_USERNAME}
          - COMMANDS_RABBITMQ_PASSWORD=${COMMANDS_RABBITMQ_PASSWORD}
          - MONGO_CONNECTION=${MONGO_CONNECTION}